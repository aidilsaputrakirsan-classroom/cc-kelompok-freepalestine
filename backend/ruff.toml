line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "W"]
# E702: gaya kode existing (multi-statement); E402: load_dotenv sebelum import di seed/test
ignore = ["E501", "E702", "E402"]

[lint.per-file-ignores]
"main.py" = ["F401"]
"seed.py" = ["F401"]
"test_main.py" = ["F401"]
